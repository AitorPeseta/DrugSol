name: drugsol-bert
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - numpy<2.0
  - pandas=2.1.*
  - pyarrow
  - tqdm
  - tokenizers
  - rdkit=2023.09.*
  - pip:
      - torch>=2.6.0
      - accelerate
      - safetensors
      - transformers>=4.30
