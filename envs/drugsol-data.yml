name: drugsol-data
channels:
  - conda-forge
dependencies:
  # --- Core ---
  - python=3.10
  - pip
  
  # --- Ciencia de Datos ---
  - numpy<2.0         
  - pandas=2.1.*
  - pyarrow
  - scikit-learn
  - matplotlib
  - seaborn
  - requests
  
  # --- Química ---
  - rdkit=2023.09.*
  - mkl 
  - mkl-service
  - xtb               
  
  # --- Pip (SIEMPRE AL FINAL) ---
  - pip:
      - mordredcommunity