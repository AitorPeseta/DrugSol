/*
 * -------------------------------------------------
 * Nextflow config file for DrugSol Prediction
 * -------------------------------------------------
 */

// 1. Project Manifest (Metadata for your TFG)
manifest {
    name            = 'drugsol-predictor'
    author          = 'Àitor Olivares Perucho'
    description     = 'Solubility predictor using XGBoost, LightGBM, Chemprop, and Regression with temperature weighting.'
    mainScript      = 'main.nf'
    nextflowVersion = '!>=22.10.1'
    version         = '1.0.0'
}

// 2. Global Parameters
params {
    // Default input/output paths can go here in the future
    outdir = "${launchDir}/results"
}

// 3. Executor & Resources
workDir = "${baseDir}/work"

// 4. Conda / Mamba Configuration
// Nextflow will automatically look for 'mamba' in the user's PATH.
conda {
    enabled  = true
    useMamba = true
    cacheDir = "${baseDir}/conda_cache" 
}

// 5. Process Specific Configuration
process {
    // Fail fast if something goes wrong, helpful for debugging
    errorStrategy = 'finish' 

    // Disable cache for the ensembling/blending step to ensure it always runs with latest inputs
    withName: '.*meta_stack_blend.*' {
        cache = false
    }
}

// 6. Profiles
// Allows you to run with: nextflow run main.nf -profile standard
profiles {
    standard {
        process.executor = 'local'
    }
    
    // If you ever move to Docker in the future
    docker {
        conda.enabled = false
        docker.enabled = true
    }
}